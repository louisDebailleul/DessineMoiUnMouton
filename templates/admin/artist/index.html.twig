{% extends 'base.html.twig' %}
{% block title %}Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1>Admin Artistes</h1>
        <div class="float-md-right">
            <a class="btn btn-primary" href="{{ path('admin.artist.new') }}" >Nouveau</a>
        </div>
        <table class="table table-dark table-striped">
            <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th colspan="3"></th>
            </tr>
            </thead>
            <tbody>
            {% for artist in artists %}
                    <tr>
                        <td>{{ artist.lastname}}</td>
                        <td>{{ artist.firstname}}</td>
                        <td>{{ artist.email}}</td>
                        <td><a class="btn btn-warning" href="{{ path("admin.artist.show",{id: artist.id}) }}" >Voir</a></td>
                        <td><a class="btn btn-primary" href="{{ path("admin.artist.edit",{id: artist.id}) }}" >Editer</a></td>
                        <td><a class="btn btn-danger" href="#" >Sup</a></td>
                    </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
